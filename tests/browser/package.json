{
  "name": "contextgc-browser-tests",
  "private": true,
  "type": "module",
  "description": "Drives the real page in a real browser. The website was inert once and a syntax check passed it; a static audit cannot see a boot() that never runs.",
  "scripts": {
    "test": "node page.mjs"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
